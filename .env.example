# Public API base URL (no trailing slash). Falls back to the host in v1.json if unset.
NEXT_PUBLIC_API_BASE_URL=https://api.qpu.buzz

# Google Analytics 4 Measurement ID
NEXT_PUBLIC_GA_MEASUREMENT_ID=G-SD7HS73TM2

# Canonical public site URL for sitemaps, robots.txt, and metadataBase
NEXT_PUBLIC_SITE_URL=https://qpu.edu.sy