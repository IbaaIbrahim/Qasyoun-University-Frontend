services:
  # frontend-dev:
  #   build:
  #     context: .
  #     dockerfile: Dockerfile
  #     target: development
  #   container_name: qpu-frontend-dev
  #   ports:
  #     - "3001:3000"
  #   volumes:
  #     - .:/app
  #     - /app/node_modules
  #     - /app/.next
  #   environment:
  #     - NODE_ENV=development
  #     - WATCHPACK_POLLING=true
  #   env_file:
  #     - .env
  #   stdin_open: true
  #   tty: true

  frontend-prod:
    build:
      context: .
      dockerfile: Dockerfile
      target: production
      network: host
      args:
        - NEXT_PUBLIC_API_BASE_URL=${NEXT_PUBLIC_API_BASE_URL:-https://api.qpu.buzz}
        - NEXT_PUBLIC_GA_MEASUREMENT_ID=${NEXT_PUBLIC_GA_MEASUREMENT_ID}
    container_name: qpu-frontend-prod
    ports:
      - "8002:3000"
    restart: always
